/**
 * api-crm-sanbox  (sim, "sanbox", sem o 'd')
 *
 * Rede de segurança do nó `add_service` do FLUXO SANDBOX, que nasceu com este
 * slug errado. Só carrega a gêmea correta `api-crm-sandbox`.
 * Motivo completo em `api-availability-sanbox/index.ts`.
 */
import "../api-crm-sandbox/index.ts";
