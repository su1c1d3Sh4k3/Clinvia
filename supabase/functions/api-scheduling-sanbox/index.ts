/**
 * api-scheduling-sanbox  (sim, "sanbox", sem o 'd')
 *
 * Rede de segurança do nó `create_appointment` do FLUXO SANDBOX, que nasceu
 * com este slug errado. Só carrega a gêmea correta `api-scheduling-sandbox`.
 * Motivo completo em `api-availability-sanbox/index.ts`.
 */
import "../api-scheduling-sandbox/index.ts";
