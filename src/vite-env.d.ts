/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

/** Injetado pelo `define` do vite.config.ts: commit curto + horário do build. */
declare const __APP_VERSION__: string;
